/**
 * Created by dbaxter on 7/25/14.
 */
define(['assets/js/modules/datetime.js'], function () {
        ruckus.modules.cron.tasks = [];

        var onesecond;
        var fiveseconds;
        var thirtyseconds;
        var oneminute;
        var fiveminutes;

        ruckus.modules.cron = function () {
                /* constructor */
        };

        ruckus.modules.cron.start = function () {

        };

        ruckus.modules.cron.stop = function () {

        };

        ruckus.modules.cron.registerTask = function (task, interval) {

        };

        ruckus.modules.cron.removeTask = function (task) {

        };
});